export const metadata = {
  title: 'Breadcrumbs - @kaynora/ui',
  description: 'A list of links displaying the path to the current page within the site\'s hierarchy.',
}

import { Section } from '@/components/section'
import { Reference } from '@/components/reference'
import { P } from '@/components/paragraph'
import {
  BreadcrumbsDisplay,
  InternalDisplay,
} from './breadcrumbsDisplay'

<Section>

# Breadcrumbs
<P>
A list of links displaying the path to the current page within the site's hierarchy.
</P>
<BreadcrumbsDisplay />
```jsx
import { Breadcrumbs } from '@kaynora/ui'

export default function () {
  return (
    <Breadcrumbs items={[
      { label: 'Home', href: '#' },
      { label: 'Components', href: '#' },
      { label: 'Core', href: '#' },
      { label: 'Breadcrumbs', href: '#' },
    ]} />
  )
}
```

## Examples

### Internal
<P>
The `internal` prop determines the props to override internal components with.
</P>
<InternalDisplay />
```jsx
import { Breadcrumbs } from '@kaynora/ui'

export default function () {
  return (
    <Breadcrumbs
      items={[
        { label: 'Home', href: '#' },
        { label: 'Movies', href: '#' },
        { label: 'Mulholland Drive', href: '#' },
      ]}
      internal={{
        typography: { color: 'primary' }
      } as any}
    />
  )
}
```

## API Reference

### Root
```jsx
<Breadcrumbs />
```

<Reference rows={[
  ['items', `{
    label: string
    href: string
  }[]`, `—`],
  ['internal', `{
    root: HTMLDivElementProps
    button: ButtonProps
    typography: TypographyProps
}`, 'undefined'],
]} />

</Section>
